protobuf>=4.25
httpx>=0.27
h2>=4.1
fastapi>=0.110
# websocket impl for the admin live feed — bare uvicorn ships none and would
# reject every upgrade handshake ("No supported WebSocket library detected").
websockets>=12
uvicorn>=0.29
